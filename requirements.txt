# Free, open-source dependencies only — no paid APIs.
yfinance>=0.2.40     # Free Yahoo Finance market data (OHLCV, news, fundamentals)
pandas>=2.0.0        # Data handling / tables
numpy>=1.24.0        # Numeric helpers
openpyxl>=3.1.0      # Excel (.xlsx) export engine
streamlit>=1.30.0    # Dashboard UI
plotly>=5.18.0       # Interactive charts in the dashboard
matplotlib>=3.7.0    # Sparkline charts for the email/report
lxml>=5.0.0          # Parse index constituents (Wikipedia) for the universe
deep-translator>=1.11.0  # Auto-translate company summaries to Hebrew (best-effort, optional)
pytest>=8.0.0        # Test suite (dev)
pytest-cov>=4.1.0    # Test coverage (dev)
