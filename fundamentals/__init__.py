"""fundamentals/ — free fundamental snapshot.

A small set of quality/valuation fields (market cap, P/E, margins, growth,
sector) via yfinance's .info. No paid APIs. Off by default in config because
per-ticker .info calls are slow; enable to enrich the export and ranking.
"""
