"""ranking_engine/ — turns metrics into a single 0-100 score.

The score blends trend structure, proximity to highs, RSI, volume, and
short-term posture so the watchlist can be ranked. Tune the weights in score.py.
"""
