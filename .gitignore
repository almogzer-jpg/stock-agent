# Secrets — never commit your email credentials
email_config.json

# Generated outputs — ignore everything in data/ EXCEPT the snapshot the
# Streamlit Cloud deploy needs to render instantly (no live scan at boot).
data/*
!data/results.csv
!data/market_overview.json
!data/closes.json
!data/events.json
!data/alerts_center.json
!data/backtest.json
!data/system_health.json
!data/universe.json
!data/reliability.json
!data/scores_today.json
!data/scores_prev.json
# keep ignoring logs, excel, caches and the timestamped outputs/
data/outputs/
data/*.log
data/*.xlsx

# Python
__pycache__/
*.pyc

# Local editor / harness config
.claude/

# Old MVP app (separate project) + review packages — never commit
stock-screener/
STOCK_AGENT_PRO_FULL_REVIEW.zip
CHATGPT_REVIEW_PACKAGE/
data/decision_audit.jsonl
data/rec_history.json
data/my_watchlist.json
